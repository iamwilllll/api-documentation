import type { Request, Response, NextFunction } from 'express';

export function createSectionController(req: Request, res: Response, next: NextFunction) {
    try {
        
    } catch (err) {
        next(err);
    }
}
